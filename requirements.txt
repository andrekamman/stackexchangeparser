# Core dependencies - required for basic functionality
lxml>=4.9.0,<6.0.0
pyyaml>=6.0,<8.0

# Parquet support - optional but recommended for Parquet output
pandas>=1.5.0,<3.0.0
pyarrow>=10.0.0,<16.0.0